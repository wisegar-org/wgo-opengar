export enum EventTypeEnum {
  Event = 'Event',
  Course = 'Course',
}

export enum EventStateEnum {
  Waiting = 'Waiting',
  Confirmed = 'Confirmed',
  Cancelled = 'Cancelled',
}

export enum AGVRoles {
  Admin = 'agvAdmin',
}
